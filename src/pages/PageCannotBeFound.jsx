import "../PageCannotBeFound.css"

const PageCannotBeFound = () => {
  return (
    <main className="error-div">
      <h2 className="page-error">This page cannot be found!</h2>
    </main>
  );
};

export default PageCannotBeFound;
